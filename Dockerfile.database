FROM mysql:8.0

# Set environment variables
ENV MYSQL_ROOT_PASSWORD=rootpassword
ENV MYSQL_DATABASE=hp_printer_shop
ENV MYSQL_USER=hp_user
ENV MYSQL_PASSWORD=userpassword

# Copy schema file
COPY db/schema.sql /docker-entrypoint-initdb.d/

# Expose MySQL port
EXPOSE 3306
